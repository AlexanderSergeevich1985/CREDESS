server:
  port: 8080

# Python CREDESS service
credess:
  python:
    service-url: http://localhost:8000

# LangChain4j configuration
langchain4j:
  open-ai:
    chat-model:
      api-key: ${OPENAI_API_KEY:your-key-here}
      model-name: gpt-4o-mini
      temperature: 0.7